# Live store (LMDB directory). The committable artifact is `claim dump` output.
.claimgraph/
*.dump.jsonl
bench/results/

# Book build artifacts (sources live in book/chapters/; `bb book` regenerates these)
book/rendered/
.clay/
.cpcache/
